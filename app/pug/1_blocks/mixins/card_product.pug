mixin card(name, prop, text)
	article.card&attributes(attributes)
		.card_img
			img(src="media/img-p/set.jpg", alt="")

		.card_content
			.card_info
				b.card_title #{name}
				span.card_prop #{prop}
				p.card_text #{text}
			
			.card_foo
				.card_price
					span.card_price-count 999
					span.card_price-symbol ₽

				.card_actions
					a(href="#").card_cart-btn: +icon("shopping-basket")